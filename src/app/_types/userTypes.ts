export type userInfo = {
    username: string,
    numArtists: number,
    profilePic: string,
}

export type track = {
    album: string,
    artist: string,
    title: string,
    url: string,
    image: string
}